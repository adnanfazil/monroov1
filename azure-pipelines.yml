trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '18.x'  # Specify the Node.js version
  displayName: 'Install Node.js'

- script: |
    npm install
  displayName: 'Install dependencies'

- script: |
    npm start
  displayName: 'Run application'

- script: |
    npm audit --audit-level=high
  displayName: 'Audit for vulnerabilities'

# Uncomment and customize the following steps if you have tests or linting
# - script: |
#     npm test
#   displayName: 'Run tests'

# - script: |
#     npm run lint
#   displayName: 'Lint code'
